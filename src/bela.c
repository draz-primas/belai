#include "core.h"

int rek(struct bela_stanje stanje, int karta){
    /* novobacenu nitko nema */
    for (int i = 0; i < 4; ++i) {
        stanje.karte[i][karta] = nema;
    }
    /* ako nije prava, nema adut */
    if (karta / 8 != )

    stanje.na_redu++;
    return 0;
}
